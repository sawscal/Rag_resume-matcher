version: '3.8'

services:
  web:
    build: .
    image: resume-job-matcher:latest
    container_name: resume_matcher_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
    restart: always
